# Masques Collector — Remote ClickHouse Configuration
#
# Copy this to .env and fill in your values:
#   cp .env.example .env
#
# The collector ships metrics and logs to your ClickHouse instance.
# If you're tunneling (e.g. SSH), point to your local tunnel endpoint.

# ClickHouse HTTP endpoint
# Examples:
#   Direct:  http://clickhouse.example.com:8123
#   Tunnel:  http://host.docker.internal:8123
CLICKHOUSE_ENDPOINT=http://host.docker.internal:8123

# ClickHouse credentials
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# Database name (auto-created if create_schema is true)
CLICKHOUSE_DATABASE=otel
