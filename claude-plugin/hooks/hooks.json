{
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
          "timeout": 30
        }
      ]
    }
  ],
  "Stop": [
    {
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Before completing this session, check if any changes were made that affect the project structure, CLI commands, build system, or implementation status documented in CLAUDE.md. If so, update CLAUDE.md to reflect the current state. Key sections to verify: Directory Structure, Build Commands, CLI Commands, Implementation Status. Only update if meaningful changes were made - don't update for minor code changes that don't affect the documented interface or structure."
        }
      ]
    }
  ]
}
