# Claude session state
.claude/masque.session.yaml
.claude/active.masque
.claude/settings.local.json

# Private masques (stored in ~/.masques/, not here)

# Local environment config
services/collector/.env

# Beads issue tracker (local project state)
.beads/

# Collector JSONL data (local telemetry for DuckDB scoring)
services/collector/data/

# Team files generated by masque TUI
*.team.yaml

# Planning & progress docs — kept locally for next steps, not shipped
# (may contain absolute paths, private-repo structure, and unreleased plans)
/docs/prd-*.md
/docs/progress*.md
/progress.md
/progress-*.md

# OS files
.DS_Store
